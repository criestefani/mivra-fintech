# LOCAL DEVELOPMENT ENVIRONMENT TEMPLATE
# Copy this file to .env.local and fill in your values
# This file is for LOCAL DEVELOPMENT ONLY and should NOT be committed

# ===== LOCAL SUPABASE (PostgreSQL + PostgREST) =====
# Use these values to connect to local Docker containers
# See docker-compose.yml for more details

SUPABASE_URL="http://localhost:3000"
SUPABASE_SERVICE_KEY="your-jwt-secret-for-development-min-32-char-abcdefghij"
SUPABASE_ANON_KEY="your-jwt-secret-for-development-min-32-char-abcdefghij"

# Direct database connection for migrations/setup
LOCAL_DB_URL="postgresql://postgres:postgres@localhost:5432/mivra_dev"
LOCAL_DB_HOST="localhost"
LOCAL_DB_PORT="5432"
LOCAL_DB_USER="postgres"
LOCAL_DB_PASSWORD="postgres"
LOCAL_DB_NAME="mivra_dev"

# ===== AVALON BROKER =====
# Use real credentials (Avalon doesn't have a local version)
# Get these from: apps/backend/.env (production values)
AVALON_API_KEY="<get_from_production_.env>"
AVALON_API_TOKEN="<get_from_production_.env>"
AVALON_USER_ID="<get_from_production_.env>"
AVALON_SYSTEM_USER_ID="<get_from_production_.env>"
AVALON_WS_URL="wss://ws.trade.avalonbroker.com/echo/websocket"
AVALON_SESSION_URL="http://api-qc.avalonbots.com:3000/session"
AVALON_CREATE_USER_URL="https://api.trade.avalonbroker.com"
AVALON_AFF_ID="430322"

# ===== REDIS (Local) =====
# For local development, use localhost
REDIS_URL="redis://localhost:6379"
REDIS_API_KEY="dev-key-no-auth"

# ===== NODE ENVIRONMENT =====
NODE_ENV="development"

# ===== FEATURE FLAGS =====
ALLOW_LOCAL_SUPABASE="true"
